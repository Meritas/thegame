(function(){
	var app = angular.module('game', [ ]);

	app.controller('GameBorder', function(){
		var vector = 1;
	});

	app.controller('FightController', function($stateParams){
		var bState = 0; //battle state; 0 - starting, 1 - ongoing, 2 - ending


	});
});